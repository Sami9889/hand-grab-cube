name: crypt
on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      file:
        description: 'File to decrypt (leave empty for whole repo)'
        required: false
        default: ''

jobs:
  encrypt-and-deploy:
    name: Encrypt and Deploy Pages
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Code Base
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT }}

      - name: Initialize Processing Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Write Crypt Script
        env:
          CRYPT_SCRIPT: ${{ secrets.CRYPT_SCRIPT }}
        run: node -e "const fs=require('fs'); fs.writeFileSync('/tmp/crypt.js', process.env.CRYPT_SCRIPT);"

      - name: Decrypt All Files
        if: github.event_name == 'push'
        env:
          IS_DISPATCH: "true"
          DECRYPT_FILE: ""
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_WORKSPACE: ${{ github.workspace }}
        run: node /tmp/crypt.js

      - name: Copy Decrypted Files for Pages
        if: github.event_name == 'push'
        run: |
          mkdir /tmp/pages-deploy
          rsync -a --exclude='.git' --exclude='node_modules' "$GITHUB_WORKSPACE/" /tmp/pages-deploy/

      - name: Write Obfuscate Script
        if: github.event_name == 'push'
        env:
          OBFUSCATE_SCRIPT: ${{ secrets.OBFUSCATE_SCRIPT }}
        run: node -e "const fs=require('fs'); fs.writeFileSync('/tmp/obfuscate.js', process.env.OBFUSCATE_SCRIPT);"

      - name: Install Terser
        if: github.event_name == 'push'
        run: cd /tmp && npm install terser

      - name: Obfuscate JS Files for Pages
        if: github.event_name == 'push'
        env:
          PAGES_DIR: /tmp/pages-deploy
        run: node /tmp/obfuscate.js

      - name: Deploy to GitHub Pages Branch
        if: github.event_name == 'push'
        env:
          PAT: ${{ secrets.PAT }}
        run: |
          cd /tmp/pages-deploy
          git init
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git remote add origin "https://x-access-token:${PAT}@github.com/${GITHUB_REPOSITORY}"
          git add -A
          git commit -m "deploy: sync obfuscated build to GitHub Pages [skip ci]"
          git push origin HEAD:gh-pages --force

      - name: Run Crypt Script
        env:
          DECRYPT_FILE: ${{ github.event.inputs.file || '' }}
          IS_DISPATCH: ${{ github.event_name == 'workflow_dispatch' }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_WORKSPACE: ${{ github.workspace }}
        run: node /tmp/crypt.js

      - name: Commit Transformed Repository Tree
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git remote set-url origin https://x-access-token:${{ secrets.PAT }}@github.com/${{ github.repository }}
          git add .
          if git diff-index --quiet HEAD --; then
            exit 0
          else
            git commit -m "security: polymorphic file structure lockdown cycle completed [skip ci]"
            git push origin main
          fi
